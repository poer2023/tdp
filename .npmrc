# 强制要求 Node/npm 版本匹配 package.json 的 engines 字段
engine-strict=true

# 使用精确版本（=）而非 ^ 或 ~，确保跨机器依赖版本一致
save-exact=true

# 加快安装速度：使用国内镜像（可选，根据网络情况调整）
# registry=https://registry.npmmirror.com

# 自动清理旧的 npm 缓存
audit-level=moderate
fund=false
