# ============================================================================
# 本地开发环境变量模板 (.env.local.example)
# ============================================================================
# 用途: 存放本地开发时的敏感配置,不应提交到 Git
#
# 使用方法:
# 1. 复制此文件为 .env.local: cp .env.local.example .env.local
# 2. 填写真实的密钥和凭证
# 3. .env.local 会被 .gitignore 忽略,不会误提交
#
# 跨机器同步:
# - 方式1: 手动复制 .env.local 到新机器
# - 方式2: 使用 iCloud/Dropbox 同步
# - 方式3: 使用密钥管理工具 (1Password CLI/Bitwarden)
# ============================================================================

# ============================================================================
# 核心配置 (必需)
# ============================================================================

# 云端数据库连接 URL
# 格式: postgresql://user:password@host:port/database?schema=public
# 示例: postgresql://tdp:abc123@db.example.com:5432/tdp_prod?schema=public
DATABASE_URL=postgresql://user:password@host:port/database?schema=public

# NextAuth 会话密钥 (长度 ≥ 32)
# 生成方法: openssl rand -base64 32
NEXTAUTH_SECRET=

# NextAuth 回调地址 (本地开发)
NEXTAUTH_URL=http://localhost:3000

# ============================================================================
# Google OAuth 认证 (必需)
# ============================================================================

# Google Cloud Console: https://console.cloud.google.com/apis/credentials
# 1. 创建 OAuth 2.0 客户端 ID
# 2. 授权的重定向 URI 添加: http://localhost:3000/api/auth/callback/google
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# ============================================================================
# 管理员配置
# ============================================================================

# 管理员邮箱白名单 (逗号分隔)
# 这些邮箱登录后自动获得管理员权限
ADMIN_EMAILS=your-email@gmail.com

# 管理端 API 密钥 (用于手动触发同步)
# 生成方法: openssl rand -hex 32
ADMIN_API_KEY=

# 凭证加密密钥 (32 字节 base64)
# 生成方法: npm run generate-key
ENCRYPTION_KEY=

# ============================================================================
# 第三方平台凭据 (可选 - 根据需要配置)
# ============================================================================

# --- Bilibili ---
# 获取方法: 登录 bilibili.com → F12 → Application → Cookies
BILIBILI_SESSDATA=
BILIBILI_BILI_JCT=
BILIBILI_BUVID3=

# --- 豆瓣 ---
# 用户 ID: 从你的豆瓣主页 URL 获取
# Cookie: dbcl2="xxx" (保留引号)
DOUBAN_USER_ID=
DOUBAN_COOKIE=

# --- Steam ---
# API Key: https://steamcommunity.com/dev/apikey
# Steam ID: https://steamid.io/
STEAM_API_KEY=
STEAM_USER_ID=

# --- HoYoverse (绝区零) ---
# Cookie: 从 hoyolab.com 获取 (包含 ltoken, ltuid)
# UID: 游戏内查看
# Region: cn_gf01 (国服) / os_asia (亚服) / os_usa (美服) / os_euro (欧服)
HOYO_COOKIE=
HOYO_UID=
HOYO_REGION=cn_gf01

# --- Uptime Kuma 监控 ---
# URL: Uptime Kuma 服务地址
# API Key: 在 Uptime Kuma 中生成
UPTIME_KUMA_URL=http://localhost:3001
UPTIME_KUMA_API_KEY=
NEXT_PUBLIC_UPTIME_KUMA_URL=

# ============================================================================
# 调试和开发 (可选)
# ============================================================================

# 开发模式调试日志
# DEBUG=prisma:query,prisma:info,prisma:warn

# 禁用 telemetry
NEXT_TELEMETRY_DISABLED=1

# ============================================================================
# 功能开关 (本地开发建议全部启用)
# ============================================================================

FEATURE_ADMIN_CREDENTIALS=on
FEATURE_ADMIN_ANALYTICS=on
FEATURE_ADMIN_GALLERY=on
FEATURE_ADMIN_POSTS=on
FEATURE_ADMIN_SYNC=on
FEATURE_ADMIN_EXPORT=on
FEATURE_ADMIN_DASHBOARD=on
FEATURE_GALLERY_INSIGHTS=on
